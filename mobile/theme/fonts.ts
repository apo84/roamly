/**
 * Loaded in root `_layout` via `@expo-google-fonts/*`; use these keys in `fontFamily`.
 */
export const fonts = {
  sans: "DMSans_400Regular",
  sansMedium: "DMSans_500Medium",
  sansBold: "DMSans_700Bold",
  display: "PlayfairDisplay_700Bold",
} as const;

export const radius = {
  md: 10,
  lg: 12,
  xl: 16,
} as const;
